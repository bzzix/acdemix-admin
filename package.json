{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "pm2:start": "pm2 start ecosystem.config.js --env production",
    "pm2:stop": "pm2 stop nuxt-app",
    "pm2:restart": "pm2 restart nuxt-app",
    "pm2:reload": "pm2 reload nuxt-app",
    "pm2:delete": "pm2 delete nuxt-app",
    "pm2:logs": "pm2 logs nuxt-app",
    "pm2:status": "pm2 status",
    "deploy": "chmod +x deploy.sh && ./deploy.sh"
  },
  "dependencies": {
    "@nuxt/devtools": "^2.6.0",
    "@nuxt/image": "^1.10.0",
    "@nuxt/image-edge": "^1.3.0-28493895.369b747",
    "@nuxtjs/i18n": "^9.5.6",
    "nuxt": "^3.17.4",
    "vue": "^3.5.14",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "sass-embedded": "^1.89.2",
    "vue-tsc": "^2.2.10"
  }
}
